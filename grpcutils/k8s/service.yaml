apiVersion: v1
kind: Service
metadata:
  name: utility-service
  labels:
    app: utility-service
spec:
  type: ClusterIP
  ports:
  - port: 50051
    targetPort: 50051
    protocol: TCP
    name: hello-grpc
  - port: 50052
    targetPort: 50052
    protocol: TCP
    name: mongo-grpc
  selector:
    app: utility-service
